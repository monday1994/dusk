using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Bimber.ObjectsMethodsExtensions
{
    public static class ListObjectExtensions
    {
        public static void ReplaceStringAtIdx(this List<string> listOfStrings,string itemToReplace)
        {
            //int idx = listOfStrings.IndexOf(itemToReplace);

            //if(idx != null)
            //{
            //    listOfStrings.ElementAt(idx)
            //}
        }
    }
}